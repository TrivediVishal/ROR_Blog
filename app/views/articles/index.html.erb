<h1>Articles</h1>

<ul>
    <% @articles.each do |article| %>
        <li>
            <!-- <a href="/articles/<%= article.id %>"><%= article.title %></a> the below line is a replacement for this -->
            <%= link_to article.title, article %>
        </li>
        <% end %>
</ul>
